{
  "name": "lunarsync",
  "version": "1.0.0",
  "description": "The ultimate place for 200+ unblocked games at school",
  "engines": {
    "npm": ">=9.0.0",
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "npm run generate-html && vite build",
    "generate-html": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "@titaniumnetwork-dev/ultraviolet": "^2.0.0",
    "@tomphttp/bare-server-node": "^2.0.5",
    "express": "^4.19.2"
  }
}